# 確定申告ダッシュボード（microCMS テンプレート）

microCMS をデータベースとして使う、**個人事業主向けの確定申告・源泉徴収ダッシュボード**のテンプレートです。

日々の売上・経費・所得控除の記録を、**e-Tax の入力欄にそのまま転記できる形で合計して表示する**ことだけに特化しています。microCMS の管理画面をそのまま入力 UI として使い、フロントは集計・確認に専念する構成です。

> **⚠️ 本システムは複式簿記の帳簿ではありません**
> 記録を確定申告の様式どおりに合計して表示するだけのツールで、法定帳簿として必要な複式簿記（借方・貸方の仕訳）の代わりにはなりません。青色申告特別控除（65万円・55万円）の適用には、別途、正規の簿記による帳簿の作成・保存が必要です。この但し書きは全ページ上部にも表示されます。

## 何ができるか

| 画面 | 内容 |
| --- | --- |
| ダッシュボード（`/`） | KPI 7指標（売上／源泉徴収税額／必要経費／所得控除／所得金額／未突合件数／登録件数）＋ 2グラフ（月別推移・科目別内訳）。支払調書と未突合の売上をアラート表示 |
| 売上・源泉徴収（`/incomes`） | **支払者 × 種目**の年間合計（第二表「所得の内訳」の1行にあたる単位）。決算書2ページ目の**月別売上**も出力。突合状況をバッジ表示 |
| 経費・決算書（`/expenses`） | **勘定科目別**の年間合計を決算書の印字順で表示。`支払総額 × 事業割合` で算出した**必要経費算入額**が転記する数字。**減価償却の対象は経費計から分離**し、区分の設定漏れを警告 |
| 所得控除（`/deductions`） | **控除種別**ごとの年間合計。第二表の**保険料区分別の内訳**、医療費の**補填額を差し引いた額**、合計をそのまま入力してよいかの**転記バッジ**を表示 |

全ページ共通で**対象年（暦年 1/1〜12/31）**を切り替えられます。確定申告は5年前まで遡って提出できるため、直近5年分を選択できます。

## 技術構成

- **Next.js 16**（App Router / Server Components）
- **React 19** / **TypeScript 5**
- **Sass Modules**（CSS フレームワーク非依存）
- **Recharts 3**（グラフ）
- **microcms-js-sdk**
- **Playwright**（E2E）
- ホスティング: **Vercel**

## セットアップ

### 前提条件

- **Node.js 18.17 以上** / **npm 9 以上**
- **microCMS アカウント**（無料プランで利用可能）
- **GitHub アカウント**（Vercel へのデプロイ時に必要）

### 1. microCMS 側の準備

#### 1-1. API を作成

以下の 5 つの API（すべてリスト形式）を作成します。

リポジトリ同梱の `microcms-template-*.json` をインポートするのが確実です。手で作る場合は以下のとおりです（`*` は必須）。

**`clients`（取引先マスタ）**

| フィールド ID | 種類 | 内容 |
| --- | --- | --- |
| `name` * | テキスト | 取引先の正式名称 |
| `address` | テキストエリア | 第二表「所在地等」への転記用 |
| `income_type` | セレクト | 第二表「所得の種類」：`事業所得` / `雑所得` / `その他（備考に記載）` |
| `note` | テキストエリア | 支払サイクルのメモ |

**`categories`（勘定科目マスタ）**

| フィールド ID | 種類 | 内容 |
| --- | --- | --- |
| `name` * | テキスト | 決算書に記載する科目名 |
| `description` | テキスト | 該当する出費のメモ |
| `sort_order` | 数値 | 決算書の印字順。小さいほど上 |

**`income`（売上・源泉徴収）**

| フィールド ID | 種類 | 内容 |
| --- | --- | --- |
| `date` * | 日時 | 発生日（サービス提供日）。**入金日ではない** |
| `client` | 参照（`clients`） | 取引先 |
| `amount` | 数値 | 売上金額（税込） |
| `tax_withheld` | 数値 | 源泉徴収税額。無ければ 0 |
| `is_verified` | 真偽値 | 支払調書と一致したか |
| `income_category` | テキスト | 第二表「種目」（原稿料・デザイン料など） |
| `paid_date` | 日時 | 入金日（参考記録用。表示するのみで集計・入金管理には使わない） |
| `memo` | テキスト | 案件名など |

**`expenses`（経費記録）**

| フィールド ID | 種類 | 内容 |
| --- | --- | --- |
| `date` * | 日時 | 支払日。**カードは引落日ではなく利用日** |
| `category` | 参照（`categories`） | 勘定科目 |
| `payee` | テキスト | 支払先名 |
| `asset_type` | セレクト | `経費（10万円未満）` / `一括償却資産（10万円以上20万円未満・3年均等）` / `少額減価償却資産の特例（30万円未満・青色）` / `減価償却資産（30万円以上）` |
| `payment_method` | セレクト | `クレジットカード` / `現金` / `銀行振込` / `事業主借（個人の財布から支払）` |
| `amount` | 数値 | 支払総額 |
| `business_ratio` | 数値 | **事業割合(%)**。全額事業なら 100 |
| `ratio_basis` | テキスト | 按分の根拠（床面積比40% など） |
| `invoice_number` | テキスト | 支払先の適格請求書発行事業者登録番号（参考記録用。課税事業者になったら使う） |
| `receipt_images` | 複数画像 | 領収書・証憑 |
| `note` | テキストエリア | 会食相手・購入目的など |

**`deductions`（所得控除・保険料）**

| フィールド ID | 種類 | 内容 |
| --- | --- | --- |
| `date` * | 日時 | 引き落とし日・寄付日 |
| `deduction_type` | セレクト | `社会保険料（国保・年金等）` / `生命保険料` / `地震保険料` / `小規模企業共済等掛金` / `医療費` / `寄付金（ふるさと納税）` |
| `insurance_category` | セレクト | 第二表の区分：`新生命保険料` / `旧生命保険料` / `介護医療保険料` / `新個人年金保険料` / `旧個人年金保険料` / `地震保険料` / `旧長期損害保険料` |
| `payee` | テキスト | 支払先・名称 |
| `amount` | 数値 | 支払金額 |
| `compensated_amount` | 数値 | 保険金などで補填される金額（医療費控除用） |
| `certificate_image` | 画像 | 控除証明書 |
| `note` | テキスト | 備考 |

> **売上の API ID は単数形の `income`** です。エンドポイント名は [`constants/index.ts`](constants/index.ts) の `ENDPOINT` にまとめてあるので、別の名前にする場合はここを変更してください。
>
> **セレクトフィールドは「複数選択」をオフ**にしてください。microCMS はセレクトの値を常に配列で返すため、コード側は先頭の値を採用しています。
>
> **`business_ratio` は「事業割合」**です。「家事按分比率」と誤解すると数字が逆になるため、フィールド名と説明文を必ず「事業で使った割合」と書いてください。

#### 1-2. API キーを取得

1. microCMS の**設定 → API キー**を開く
2. **本番用 API キー**（GET 権限）をコピーして保管

### 2. ローカル環境の構築

```bash
git clone https://github.com/your-username/income-application.git
cd income-application
npm install
```

リポジトリのルートに `.env.local` を作成します：

```bash
MICROCMS_SERVICE_DOMAIN=xxxxx   # https://xxxxx.microcms.io の xxxxx
MICROCMS_API_KEY=xxxxxxxxxx     # microCMS の API キー（1-2 で取得）
BASE_URL=http://localhost:3000
```

> **API キーの扱い**：`.env.local` は Git でコミットしないでください（`.gitignore` に含まれています）

```bash
npm run dev
```

ブラウザで [http://localhost:3000](http://localhost:3000) を開き、KPI とグラフが表示されることを確認します。microCMS にまだデータがない場合は 0 件の空状態で表示されます。

### 3. 本番環境へのデプロイ（Vercel）

1. GitHub にプッシュ
2. [Vercel](https://vercel.com) で**新規プロジェクト → GitHub リポジトリをインポート**
3. **環境変数を設定**：`MICROCMS_SERVICE_DOMAIN` / `MICROCMS_API_KEY` / `BASE_URL`
4. **デプロイ**

### 4. テスト

```bash
npx playwright test
```

ナビゲーションの疎通に加えて、**集計の整合性**（差引金額 = 売上 − 必要経費、支払総額 = 必要経費 + 家事分 など）を検証しています。

## 集計の考え方

申告書に転記する数字なので、丸め方と集計基準を明示しています。

### 必要経費算入額

```
必要経費算入額 = 支払総額 × (business_ratio ÷ 100)
```

決算書の損益計算書に載せるのは**この値**です。家事按分した差額（家事分）は経費になりません。**1円未満は明細ごとに切り捨て**ています（合算してから丸めると端数がズレるため）。`business_ratio` 未設定の明細は 100%（全額事業用）として扱います。

### 経費区分と減価償却

`asset_type` が **`経費（10万円未満）`（または未設定）の明細だけ**が「経費計」に入ります。それ以外は減価償却の対象として分離し、`/expenses` の「減価償却の対象」セクションに取得価額を表示します。**当年の償却額は計算していません**（決算書3ページ目で算出してください）。

`asset_type` が「経費」のまま 10万円以上になっている明細は、区分の設定漏れとして警告します（[`AssetTypeAlert`](components/Notice/AssetTypeAlert.tsx)）。判定基準は `DEPRECIABLE_THRESHOLD`、区分ごとの取り扱いは `ASSET_TYPE_GUIDE` にあります。

### 集計期間

確定申告の集計単位は**暦年（1/1〜12/31）固定**です。各 API の `date` フィールドで microCMS 側を絞り込むため、KPI・グラフ・明細がすべて同じスライスになります。`date` が未入力のレコードは集計対象に入りません。

### 日付は JST 固定

[`libs/datetime.ts`](libs/datetime.ts) で日本時間に固定しています。Vercel のサーバー TZ は UTC のため、素の `dayjs()` だと年末年始 0:00〜9:00 の記録が前年に集計されてしまいます。

### 源泉徴収税額

`tax_withheld` に入力された値をそのまま合計します。検算はしていません（源泉徴収は原則 10.21%、100万円超部分は 20.42%）。

### 入金管理は対象外

`income.paid_date`（入金日）は表示するだけの参考情報で、集計や入金状況の判定には使いません。売上は発生日（`date`）を基準に全額を計上済みとして扱います。入金・未入金の管理、督促、キャッシュフロー管理は本システムの対象外です（あくまで確定申告の転記を効率化するためのツールという位置づけのため）。

## 申告書の様式との対応と、扱わない範囲

本システムは**転記元**であり、申告書のすべての欄を賄うものではありません。どの欄に対応し、どこが対応していないかを明示します。

### 対応している欄

| 様式 | 欄 | 画面 |
| --- | --- | --- |
| 決算書 1ページ目 損益計算書 | 売上（収入）金額 | `/incomes` |
| 決算書 1ページ目 損益計算書 | 経費の各科目（決算書の印字順に整列）／経費計 | `/expenses` |
| 決算書 2ページ目 | 月別売上（収入）金額 | `/incomes` |
| 決算書 3ページ目 | 減価償却の対象になる資産の一覧（**取得価額まで。償却計算は別途**） | `/expenses` |
| 申告書 第二表 | 所得の内訳（所得の種類／種目／支払者の名称・所在地／収入金額／源泉徴収税額） | `/incomes` |
| 申告書 第一表・第二表 | 社会保険料控除／小規模企業共済等掛金控除（**支払額＝控除額**） | `/deductions` |
| 申告書 第二表 | 生命保険料・地震保険料の**区分別の内訳** | `/deductions` |
| 医療費控除の明細書 | 支払った医療費 − 保険金などで補填される金額（**足切り前**） | `/deductions` |
| 申告書 第一表・第二表 | 寄附金控除（**支払額のみ。2,000円の足切り前**） | `/deductions` |

### 扱わない欄（画面上にも明示されます）

「画面に出ない＝0円」という誤解が転記漏れの主因になるため、各ページに [`Coverage`](components/Notice/Coverage.tsx) として一覧表示しています。定義は [`constants/index.ts`](constants/index.ts) の `UNCOVERED_ITEMS` です。

- **売上原価**（期首・期末棚卸高、仕入金額） — 棚卸を持たない業態を前提としています
- **減価償却費の計算** — 資産の一覧と取得価額は出しますが、耐用年数・償却方法・当年分の償却額は扱いません
- **地代家賃・利子割引料の内訳**（3ページ目）、**給料賃金・専従者給与の内訳**（2ページ目）
- **貸借対照表**（4ページ目。青色申告特別控除65万円の適用に必須）
- **青色申告特別控除額の計算** — ダッシュボードの「青色申告特別控除前の所得金額」までを表示します
- **各控除額の計算** — 生命保険料・地震保険料の計算式、医療費と寄附金の足切り
- **人的控除**（基礎・配偶者・扶養）、**雑損控除**、寡婦・ひとり親・勤労学生・障害者控除
- **税額控除**（住宅ローン控除・配当控除・外国税額控除など）、**予定納税額**
- **消費税の申告**（`expenses.invoice_number` は保持していますが集計に使っていません）

### 控除は「支払額」であって「控除額」ではない

`/deductions` の各種別には**転記バッジ**を出しています。

- **支払額＝控除額** … 社会保険料控除、小規模企業共済等掛金控除。表示の合計をそのまま入力できます
- **要計算・区分** … 生命保険料、地震保険料、医療費、寄附金。計算式・足切り・区分記入が挟まります

注意文は [`constants/index.ts`](constants/index.ts) の `DEDUCTION_GUIDE` にまとまっています。

## 残っているスキーマ拡張の候補

第二表の転記に必要なフィールドは一通り揃っています。以下は課税事業者になった場合などに必要になるものです。

### `expenses` / `income` に `tax_rate`（セレクト：10% / 8%軽減 / 対象外）

`income.amount` は「売上金額(税込)」固定なので、税込経理しかできません。消費税の申告（2割特例でも売上税額の集計が要る）には税率区分が必要です。

### `expenses` に `is_electronic`（真偽値）

電子帳簿保存法では、メールや Web で受領した請求書は**電子データのまま保存する義務**があります。紙のレシートをスキャンした場合とは要件が違うため、区別できると安全です。

## カスタマイズの入口

| やりたいこと | 触る場所 |
| --- | --- |
| API のエンドポイント名を変える | [`constants/index.ts`](constants/index.ts) の `ENDPOINT` |
| 所得控除の表示順を変える | [`constants/index.ts`](constants/index.ts) の `DEDUCTION_TYPE_ORDER`（確定申告書 第一表の並び順） |
| 控除ごとの転記の注意文 | [`constants/index.ts`](constants/index.ts) の `DEDUCTION_GUIDE` |
| 経費科目の並び順 | microCMS の `categories.sort_order`。未設定なら [`constants/index.ts`](constants/index.ts) の `EXPENSE_CATEGORY_ORDER` から科目名で推定し、どちらでも決まらなければ末尾＝空欄行扱い |
| 減価償却の判定基準・区分ごとの説明 | [`constants/index.ts`](constants/index.ts) の `DEPRECIABLE_THRESHOLD` / `ASSET_TYPE_GUIDE` |
| 第二表の保険料区分の並び順 | [`constants/index.ts`](constants/index.ts) の `INSURANCE_CATEGORY_ORDER` |
| 「集計していない欄」の文面 | [`constants/index.ts`](constants/index.ts) の `UNCOVERED_ITEMS` |
| 選択できる年数 | [`constants/index.ts`](constants/index.ts) の `YEAR_OPTIONS_COUNT` |
| グラフの配色 | [`components/Dashboard/chartTheme.ts`](components/Dashboard/chartTheme.ts) |
| 集計ロジック | [`libs/analytics.ts`](libs/analytics.ts)（副作用のない純関数のみ） |
| 但し書きの文面 | [`components/Notice/index.tsx`](components/Notice/index.tsx) |
| サイドメニューの項目 | [`components/GlobalNav/index.tsx`](components/GlobalNav/index.tsx) の `NAV_ITEMS` |

集計表は [`components/List/DataTable.tsx`](components/List/DataTable.tsx) と [`components/Dashboard/SummarySection.tsx`](components/Dashboard/SummarySection.tsx) を共有しているため、**API を1つ増やす際は「集計関数と列定義とページ」を足すだけ**で同じ見た目の画面が作れます。

## 設計上のポイント

- **グラフは値をツールチップに閉じ込めない**。各グラフに「数値で見る」テーブルを併設し、色覚多様性・スクリーンリーダー・印刷のいずれでも値が読めるようにしています。配色も色覚シミュレーション上の識別性を検証済みです。
- **状態は色だけで伝えない**。突合状況・入金状況のバッジは必ず文言（`確認済` / `未確認` など）を伴います。
- **データ0件でも壊れない**。空状態の表示とゼロ埋めした月次軸を用意しています。
- **集計は全件取得**。申告用の合計値なのでページングせず、100件ずつ辿って全件を合算します（暴走防止に取得回数の上限あり）。

---

## 以下拡張開発例

### 支払調書との突合フロー

現状 `is_verified` は microCMS 側での手動チェックです。年明けに届く支払調書の金額を入力して自動照合し、差額があれば警告する、という流れにすると精度が上がります。

### 画面からのデータ入力

microCMS の管理画面を使わず、ダッシュボードから直接「入金済みにする」「突合済みにする」を操作したい、というのは頻出の要望です。microCMS の **Write API** を Server Actions 経由で叩く構成にすると、閲覧専用から「業務が回る画面」に変わります。

### 領収書 OCR / レシート取り込み

スマホで撮った領収書を microCMS にアップロードし、OCR で日付・金額・支払先を下書き登録する。経費入力の手間が最も大きい部分なので効果が出やすいところです。

### 控除額の自動計算

現在は**支払金額の合計**を表示するに留めています。生命保険料・地震保険料・医療費・寄付金は計算式や足切り額を通して控除額が決まるため、各控除の計算式を実装すれば申告書の該当欄へそのまま転記できます。

### インボイス・消費税申告への対応

`expenses.invoice_number` は保持していますが集計には使っていません。課税事業者向けに、税率区分（8% / 10%）フィールドの追加、2割特例・簡易課税での納税額試算まで広げられます。

### 帳簿の出力

CSV / Excel での仕訳データ出力、e-Tax の CSV 取込形式への変換など。「結局この画面を見ながら手入力する」を減らす方向の拡張です。

### 通知・リマインド

- 入金予定日を過ぎた売上の督促リマインド
- 予定納税・住民税・国民健康保険料の納付期限通知
- 年末の控除証明書の到着チェック

microCMS の Webhook と Vercel Cron を組み合わせると、追加のサーバーなしで実装できます。

### ログイン・権限管理

現状は URL を知っていれば誰でも閲覧できます。**金額情報を扱うため、公開環境では必ず保護してください。** Vercel の Password Protection（最小構成）か、Auth.js による認証の追加が現実的な選択肢です。

---

## 開発のご相談

上記のような拡張開発、および **react, Next.jsに限らず、LaravelなどのPHPフレームワークを使ったサイト・業務システムの開発全般**を承っています。

- テンプレートをベースにした自社向けカスタマイズ
- 認証・権限管理、入力機能ほか外部サービス連携の追加
- 既存ワークフローのシステムリニューアル
- Webサイト制作、業務システム開発、保守運用

「どこから手を付けるべきか」「この要件は現実的か」といった段階のご相談でも構いません。

**▶ [お問い合わせはこちら（株式会社創新ラボ）](https://soushin-lab.co.jp/contact)**

---

## ライセンス

MIT License
